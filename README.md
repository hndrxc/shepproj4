# shepproj4

Minimal Flutter starter for Android, iOS, web, Linux, macOS, and Windows.

## Run

With Flutter installed and a device or emulator available:

```sh
flutter pub get
flutter run
```

For web, use `flutter run -d chrome`.

Start editing in `lib/main.dart`. Dependencies live in `pubspec.yaml`.

## Check

```sh
flutter analyze
```
